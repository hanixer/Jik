(define list (lambda args args))

(define map
  (lambda (f l)
    (if (null? l)
        l
        (cons (f (car l))
              (map f (cdr l))))))

(define user
  (lambda (l n)
  ;  (list
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)
    (map (lambda (x) n) l)))
  ; ))

(define lower
  (lambda (l)
    (map (lambda (x) (user l 10926)) l)))

(lower (list 51 55))